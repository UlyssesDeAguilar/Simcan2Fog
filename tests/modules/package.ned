// Best practice, define a ned path that helps the logical link in the project
package s2f.tests.modules;

// Best practice, define the namespace as well for c++ classes
// This avoids namespaces overrides and weird behaviour
@namespace(s2f::tests);